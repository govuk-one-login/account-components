openapi: 3.0.3
info:
  version: 9.5.2
  title: "@fastify/swagger"
components:
  schemas: {}
paths:
  /healthcheck:
    get:
      responses:
        "200":
          description: Default Response
  /robots.txt:
    get:
      responses:
        "200":
          description: Default Response

